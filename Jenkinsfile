pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo 'Building the Real world Application via pipeline SCM method'
            }
        }
        
        stage('Test') {
            steps {
                echo 'Testing the Real world Application via pipeline SCM method'
            }
        }
        
        stage('Deploy') {
            steps {
                echo 'Deploying the Real world Application via pipeline SCM method'
            }
        }
    }
}
